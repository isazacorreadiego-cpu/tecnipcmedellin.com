# La portada debe verse como / y no como /index.html.
#
# Se resuelve aquí, en el servidor, en lugar de cambiar los enlaces del HTML a
# href="/": así el sitio sigue abriéndose con doble clic desde el disco, donde
# una barra suelta apuntaría a la raíz del computador y no a la portada.
/index.html    /    301
